apiVersion: v1
kind: Service
metadata:
  name: {{ include "dcsim.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "dcsim.labels" . | nindent 4 }}
spec:
  selector: {{- include "dcsim.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
