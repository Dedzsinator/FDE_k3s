replicaCount: 1

image:
  repository: dpage/pgadmin4
  tag: "8.6"
  pullPolicy: IfNotPresent

auth:
  email: "admin@fde.local"
  password: "CHANGEME"

resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "512Mi"

storage:
  data: 1Gi

service:
  port: 80

global:
  storageClass: ""

nodeSelector: {}
tolerations: []
affinity: {}
