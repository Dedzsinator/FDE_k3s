apiVersion: v1
kind: Service
metadata:
  name: {{ include "timebase.fullname" . }}-collector
  namespace: {{ .Release.Namespace }}
  labels: {{- include "timebase.labels" . | nindent 4 }}
spec:
  selector: {{- include "timebase.collector.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.collector.port }}
      targetPort: {{ .Values.service.collector.port }}
      name: http
